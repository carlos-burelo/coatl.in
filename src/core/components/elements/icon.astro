---
const { name } = Astro.props;

const icons = import.meta.glob<string>("/src/core/icons/*.svg", {
    query: "?raw",
    import: "default",
    eager: true,
});

const iconPath = `/src/core/icons/${name}.svg`;
const icon = icons[iconPath];
---

<Fragment set:html={icon} />
