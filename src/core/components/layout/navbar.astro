---
import Cmd from "../elements/cmd.astro";
---

<nav>
    <h2>Carlos Burelo</h2>
    <Cmd />
</nav>

<style>
    @keyframes scroll-collapse {
        to {
            height: 60px;
            border: solid 1px var(--border-color);
        }
    }
    nav {
        top: 0;
        display: grid;
        grid-auto-flow: column;
        justify-content: space-between;
        align-content: center;
        background-color: var(--background-color-navbar);
        backdrop-filter: blur(5em);
        position: sticky;
        animation: scroll-collapse linear both 0.2s;
        animation-timeline: scroll();
        height: 100px;
        animation-range: 0px 100px;
        border: solid 1px transparent;
        z-index: var(--z-index-navbar);
    }
</style>

<!-- ---
import Cmd from "../elements/cmd.astro";

const { title } = Astro.props;
---

<nav>
    <div>
        <h2 class="overlap">{title}</h2>
        <h2 class="original">Carlos Burelo</h2>
    </div>
    <Cmd />
</nav>

<style>
    @keyframes scroll-collapse {
        to {
            height: 60px;
            border: solid 1px var(--border-color);
        }
    }

    @keyframes hide-title {
        to {
            opacity: 0;
            height: 0;
            line-height: 0;
        }
    }

    @keyframes show-title {
        to {
            opacity: 1;
        }
    }

    nav {
        top: 0;
        display: grid;
        grid-auto-flow: column;
        justify-content: space-between;
        align-content: center;
        background-color: var(--background-color-navbar);
        backdrop-filter: blur(5em);
        position: sticky;
        animation: scroll-collapse linear both 0.2s;
        animation-timeline: scroll();
        height: 100px;
        animation-range: 0px 100px;
        border: solid 1px transparent;
        z-index: var(--z-index-navbar);
    }
    div {
        display: grid;
        align-items: center;
        overflow: hidden;
    }

    .original {
        position: absolute;
        opacity: 1;
        animation: hide-title linear both 0.1s;
        animation-timeline: scroll();
        animation-range: 0px 100px;
        font-size: 1.5em;
    }

    .overlap {
        position: absolute;
        opacity: 0;
        animation: show-title linear both 0.2s;
        animation-timeline: scroll();
        animation-range: 0px 100px;
        text-wrap: initial;
        font-size: 1em;
        overflow: hidden;
        text-overflow: ellipsis;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        max-width: calc(100vw - (2 * var(--body-inner-separation)) - 5em);
    }
</style> -->
